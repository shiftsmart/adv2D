 
using UnityEngine;

public class controllerNormal : controller
{
    // Start is called before the first frame update
    void Start()
    {
        



    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
